# Julia — resolved manifest + compiled cache are not committed (per the CognitiveSubstratesAI convention)
Manifest.toml
/compiled/
*.jl.cov
*.jl.*.cov
*.jl.mem
/docs/build/

# MORK / .act zero-copy persistence + any runtime ledger dumps
*.act
*.ledger.jsonl

# Warm dev session runtime (tools/warm_session.jl ↔ tools/warm_send.sh handshake files:
# ready/seq/done/in.jl/out.txt). Process-local scratch, never source. Mirrors Core/.gitignore:102.
.warm/

# OS
.DS_Store
